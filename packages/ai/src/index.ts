/**
 * @job-copilot/ai
 *
 * Will host the LLMProvider (generateText/generateStructured) and
 * EmbeddingProvider (embedText/embedBatch) interfaces plus their
 * concrete implementations (Phase 8+, per Phase 0 amendment #16).
 * Empty in Phase 1 — no AI wiring or API keys touched yet.
 */
export {};
