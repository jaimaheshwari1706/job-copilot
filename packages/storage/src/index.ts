/**
 * @job-copilot/storage
 *
 * Will host the object-storage abstraction (private storageKey + short-lived
 * signed URL generation) used by resume upload/download (Phase 4+, per
 * Phase 0 amendment #3). Empty in Phase 1 — no file handling yet.
 */
export {};
