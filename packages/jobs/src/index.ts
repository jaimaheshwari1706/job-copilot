/**
 * @job-copilot/jobs
 *
 * Will host the JobProvider interface, provider adapters (demo + real),
 * normalization, and duplicate-confidence detection (Phase 5+). Empty in
 * Phase 1 — no job ingestion logic yet.
 */
export {};
